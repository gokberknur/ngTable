import { TfsCellDirective } from './tfs-cell.directive';

describe('TfsCellDirective', () => {
  it('should create an instance', () => {
    const directive = new TfsCellDirective();
    expect(directive).toBeTruthy();
  });
});
